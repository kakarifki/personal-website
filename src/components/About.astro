---
const highlights = [
  { icon: "fas fa-code", text: "Software Engineer" },
  { icon: "fas fa-graduation-cap", text: "Bootcamp Graduate" },
  { icon: "fas fa-location-dot", text: "Based in Bekasi" },
];
---

<section id="about" class="py-24 relative">
  <div class="container mx-auto px-4">
    <div class="max-w-4xl mx-auto">
      <!-- Section Title -->
      <div class="text-center mb-16">
        <h2 class="text-4xl md:text-5xl font-bold mb-4">
          About <span class="gradient-text">Me</span>
        </h2>
        <div class="w-24 h-1 bg-gradient-to-r from-[#f1b814] to-[#bd1e51] mx-auto rounded-full"></div>
      </div>

      <!-- Main Card -->
      <div class="glass rounded-3xl p-8 md:p-12">
        <!-- Highlights -->
        <div class="flex flex-wrap justify-center gap-4 md:gap-8 mb-10">
          {highlights.map((item) => (
            <div class="flex items-center gap-2 px-4 py-2 rounded-full bg-white/5 border border-white/10">
              <i class={`${item.icon} text-[#f1b814]`}></i>
              <span class="text-gray-300 font-medium">{item.text}</span>
            </div>
          ))}
        </div>

        <!-- Bio -->
        <div class="space-y-6 text-center">
          <p class="text-xl md:text-2xl font-bold text-white leading-relaxed">
            From <span class="text-[#f1b814]">curiosity</span> to <span class="text-[#bd1e51]">code</span>.
          </p>
          
          <p class="text-lg text-gray-300 leading-relaxed max-w-2xl mx-auto">
            Started taking apart computers in middle school, now I build digital solutions.
            Graduated from <span class="text-[#f1b814] font-semibold">Bearmentor.com</span> bootcamp, 
            ready to create innovative software.
          </p>

          <p class="text-lg text-gray-400 leading-relaxed max-w-2xl mx-auto">
            I believe in continuous learning and building solutions that matter.
          </p>
        </div>

        <!-- CTA -->
        <div class="text-center mt-10">
          <a 
            href="#projects" 
            class="inline-flex items-center gap-2 px-6 py-3 bg-gradient-to-r from-[#265791] to-[#bd1e51] text-white font-semibold rounded-full hover:shadow-lg hover:shadow-[#265791]/25 transition-all duration-300 hover:-translate-y-1"
          >
            <span>View My Projects</span>
            <i class="fas fa-arrow-right"></i>
          </a>
        </div>
      </div>
    </div>
  </div>
</section>
