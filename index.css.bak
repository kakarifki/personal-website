/* index.css */
@tailwind base;
@tailwind components;
@tailwind utilities;

body {
  @apply bg-[#fefefe]; /* Lightest color as background */
  @apply text-[#265791]; /* Darkest color as text */
}

h1, h2, h3 {
  @apply text-[#265791];
}

a {
  @apply text-[#f1b814]; /* Medium color for links */
  @apply hover:underline;
}

.button {
  @apply bg-[#265791];
  @apply text-white;
  @apply px-4;
  @apply py-2;
  @apply rounded;
  @apply hover:bg-[#bd1e51];
}

header {
  z-index: 1000; /* Ensure header is on top */
}

/* Responsive Styles */
@media (max-width: 768px) {
  .container {
    @apply px-4;
  }
  header nav {
    @apply flex flex-col;
  }
  header nav a {
    @apply my-2;
  }
  #home {
    @apply flex flex-col;
  }
  #home div:first-child {
    @apply w-full mb-4;
  }
  #home div:last-child {
    @apply w-full;
  }
  #about .my-8 {
    @apply my-4;
  }
  #about .text-justify {
    @apply text-center;
  }
  #experience .grid {
    @apply grid-cols-1;
  }
  #project .grid {
    @apply grid-cols-1;
  }

}
